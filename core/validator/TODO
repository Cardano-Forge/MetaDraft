
- [ ] Add a value length check (each value in the object must be 64 or less) - Should be done.
- [ ] Casing that allows Title case + space (like this must be valid: "Body Color")
- [ ] Alphanumeric must allow spaces as well (Like this must be valid: "Body Color")
- [ ] Check of the image is an actual image (PNG or GIF), video should not be allowed
- [ ] Check duplicated assetName, this one is still to be done.
- [x] Fix asset name when using the execute once
- [x] Check if the hardcoded values are using the `[{metadata: {}, assetName: ""}]` structure
